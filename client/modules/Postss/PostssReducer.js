// Import Actions
import {  } from './PostssActions';

// Initial State
const initialState = {};

const PostssReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default PostssReducer;
